apiVersion: v1
kind: ConfigMap
metadata:
  name: snake-config
data:
  REDIS_HOST: "redis"        # matches the Redis Service name below (K8s DNS)
  REDIS_PORT: "6379"
  CORS_ORIGINS: "*"
  LEADERBOARD_KEY: "snake:leaderboard"
