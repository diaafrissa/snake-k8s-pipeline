# Copy this file to `.env` (same folder as docker-compose.yml) and set a
# real password. `.env` itself is git-ignored on purpose - never commit
# real credentials, even for a local demo project.
#
#   cp .env.example .env

REDIS_PASSWORD=change-me-to-something-strong
